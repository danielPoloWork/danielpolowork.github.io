---
description: Sync portfolio cards from GitHub repos (pbr-/egl-/pgs-) into the static page
---

Pre-render the portfolio cards as static HTML from the user's GitHub repos. This is an
**author-time, local** sync — no client-side fetch, no GitHub Action. You (the agent)
fetch the data and write static HTML + i18n keys, idempotently, between markers.

## 1. Fetch the repo list

Use the `gh` CLI (already authenticated as `danielPoloWork`):

```
gh repo list danielPoloWork --no-archived --source --limit 200 \
  --json name,description,url,primaryLanguage,repositoryTopics,stargazerCount,pushedAt,homepage
```

Keep only repos whose `name` starts with one of these prefixes (skip forks/archived):

| prefix | section container (`data-repo-prefix`) | marker id |
|--------|-----------------------------------------|-----------|
| `pbr-` | `#portfolio`            (Samples)        | `pbr` |
| `egl-` | `#portfolio-lib`        (Libraries)      | `egl` |
| `pgs-` | `#portfolio-pro`        (Professional)   | `pgs` |

Other repos (e.g. `coding-challenges`, `danielPoloWork`, the site repo, Mycelium) are
**not** managed here — leave the Mycelium (08) and Challenge (09) sections untouched.

## 2. Resolve per-repo copy (option A)

For each matched repo, build: **title**, **tags**, **rlang badge**, **3-language description**.

1. Try to read `.portfolio.json` from the repo:
   `gh api repos/danielPoloWork/<name>/contents/.portfolio.json --jq '.content' | base64 -d`
   Expected shape (authoritative when present):
   ```json
   {
     "title": "C++ Memory Pool",
     "tags": ["Memory Pool", "Low-latency", "Allocator"],
     "order": 1,
     "description": { "en": "...", "ja": "...", "zh": "..." }
   }
   ```
2. If **absent** (404): derive a fallback and **propose it to the user for approval
   before writing**:
   - **title** = humanize the slug minus prefix, applying the alias map:
     `cpp→C++, ts→TypeScript, js→JavaScript, py→Python, k8s→Kubernetes, api→API,
      cli→CLI, db→DB, ai→AI, rag→RAG, llm→LLM` (others Title-Cased).
   - **rlang** = `primaryLanguage.name`.
   - **tags** = up to 3 from `repositoryTopics` (Title-Cased), else the language.
   - **description.en** = a tightened one-sentence version of the GitHub `description`.
   - **description.ja / .zh** = your own faithful translations of the EN sentence.
   Show the drafted EN/JA/ZH copy and only proceed once approved.

Link target = `homepage` if set, else `url`. Sort cards by `.portfolio.json` `order`
(if present), then `stargazerCount` desc, then `pushedAt` desc.

## 3. Write the cards (idempotent, between markers)

In `index.html`, replace everything between `<!-- repos:<id>:start ... -->` and
`<!-- repos:<id>:end -->` for each section. Card template (link card):

```html
      <a class="repo" href="<LINK>" target="_blank" rel="noopener">
        <span class="rico"><svg ...section icon...></svg></span>
        <span class="repo-main"><h4><TITLE> <span class="ext">↗</span></h4><div class="rtags"><T1> · <T2> · <T3></div><div class="rdesc" data-i18n="repo.<name>.desc"><EN description></div></span>
        <span class="rlang"><LANG></span>
      </a>
```

Section icons (keep consistent): pbr = package (`M21 16V8a2 2 0 0 0-1-1.73...`),
egl = book (`M4 19.5A2.5 2.5 0 0 1 6.5 17H20...`), pgs = layers
(`<rect x="3" y="3".../>` grid). Titles and tags are **inline only** (not translated —
matches existing cards); only `rdesc` carries `data-i18n`.

If a bucket has **zero** repos, write the original placeholder card instead (the
`repo repo--soon` block with `data-i18n="repo.7"` for pbr, `repo-lib.1` for egl,
`repo-pro.1` for pgs, and `data-i18n="repo.soon"` badge), so the section never looks
broken.

## 4. Write the translations (between i18n markers)

In `src/dist-20265315/scripts/i18n.js`, inside **each** language object (`en`, `ja`,
`zh`) there is a region:

```
    // repos:start (auto-generated by /sync-portfolio — do not edit by hand)
    ...
    // repos:end
```

Replace its contents with one `"repo.<name>.desc": "<description in that language>",`
line per managed repo. Keep keys sorted by repo name for stable diffs. Note the i18n
path points at the **version-stamped dist folder** referenced by `index.html`'s
`<script>` tags — if that folder was renamed since last sync, target the current one.

## 5. Verify & report

- `node -e "global.window={};require('./src/dist-<ver>/scripts/i18n.js');..."` to confirm
  the file still parses and all three languages have equal key counts.
- Confirm every `data-i18n="repo.<name>.desc"` in `index.html` has a matching key in all
  three dicts.
- Report: **added / updated / removed** repos per section, and flag any repo that fell
  back to auto-generated copy (no `.portfolio.json`) so the user can curate it later.

Do **not** commit or push unless the user asks.
